[package]
name = "bincode_compatibility"
version = "3.0.0"
edition = "2024"
rust-version = "1.94"
publish = false

[dependencies]
bincode = { path = "..", features = ["serde"] }
serde = { workspace = true, features = ["derive"] }
rand = { workspace = true }
