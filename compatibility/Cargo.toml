[package]
name = "bincode_compatibility"
version = "3.0.0"
edition = "2024"
rust-version = "1.93.0"
publish = false

[dependencies]
bincode = { git = "https://github.com/earthlings-dev/bincode.git", features = ["serde"] }
serde = { workspace = true, features = ["derive"] }
rand = { workspace = true }
